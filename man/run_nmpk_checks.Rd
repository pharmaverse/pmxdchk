% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_checks.R
\name{run_nmpk_checks}
\alias{run_nmpk_checks}
\title{Run NONMEM dataset quality checks}
\usage{
run_nmpk_checks(
  data,
  mapping = NULL,
  thresholds = default_thresholds(),
  study_type = "All"
)
}
\arguments{
\item{data}{A data frame of a NONMEM-style (NMPK) dataset.}

\item{mapping}{Optional named character vector mapping canonical variable
names to user column names (`c(ID = "USUBJID", TIME = "TAFD")`). When
`NULL`, columns are assumed to already use canonical names.}

\item{thresholds}{A list of thresholds; see [default_thresholds()].}

\item{study_type}{Character vector of confirmed study types used to gate
conditional checks. Defaults to `"All"`.}
}
\value{
A tibble with one row per check: `check_id`, `domain`, `severity`,
  `status`, `n_flagged`, `message`. The full [new_check_result()] objects are
  attached as the `"results"` attribute for the app and reporting layers.
}
\description{
Headless entry point and the same path the Shiny app uses. Applies the
variable mapping, orders checks by their prerequisites, evaluates skip
conditions, runs each check, and returns a tidy findings tibble.
}
\examples{
df <- data.frame(
  ID = c(1, 1), TIME = c(0, 1), EVID = c(1, 0),
  MDV = c(1, 0), DV = c(0, 5), AMT = c(100, 0)
)
run_nmpk_checks(df)
}
